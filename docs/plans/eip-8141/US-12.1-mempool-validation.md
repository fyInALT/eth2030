# US-12.1 — Mempool Validation & DoS Mitigation

**Epic:** EP-12 Mempool Validation & DoS Mitigation
**Total Story Points:** 14
**Sprint:** 4

> **As a** node operator,
> **I want** the transaction pool to apply ERC-7562-inspired restrictions on VERIFY frames,
> **so that** the node is protected from mass-invalidation DoS attacks.

**INVEST:** I ✓ | N ✓ | V ✓ | E ✓ | S ✓ | T ✓

---

## Tasks

### Task 12.1.1 — Validation Frame Opcode Restrictions

| Field | Detail |
|-------|--------|
| **Description** | **Only VERIFY frames are executed during mempool simulation.** DEFAULT and SENDER frames are NOT run. ERC-7562-inspired restrictions for VERIFY frames: (a) block-context opcodes forbidden: `TIMESTAMP`, `NUMBER`, `BASEFEE`, `DIFFICULTY`/`PREVRANDAO`, `GASLIMIT`, `COINBASE`, `BLOBHASH`; (b) external introspection forbidden: `BALANCE`, `EXTCODESIZE`, `EXTCODECOPY`, `EXTCODEHASH`, `SELFBALANCE`; (c) cross-account storage forbidden; (d) `DELEGATECALL` and `CALLCODE` forbidden. Simulation runs until `APPROVE` is called. Restrictions apply only during mempool, not block execution. |
| **Estimated Effort** | 5 story points |
| **Assignee/Role** | Core Protocol Engineer |
| **Testing Method** | (1) VERIFY + `TIMESTAMP` → rejected. (2) VERIFY + external `BALANCE` → rejected. (3) VERIFY reading own storage → accepted. (4) VERIFY + `APPROVE(0x2)` no restrictions → accepted. (5) Block processing allows all opcodes. (6) DEFAULT/SENDER NOT executed during mempool. |
| **Definition of Done** | Tests pass; VERIFY-only execution confirmed; restricted opcodes documented; reviewed. |

### Task 12.1.2 — Validation Frame Gas Limit

| Field | Detail |
|-------|--------|
| **Description** | Enforce max gas limit for VERIFY frames during mempool validation (configurable, e.g., `MAX_VALIDATION_GAS = 100_000`). |
| **Estimated Effort** | 2 story points |
| **Assignee/Role** | Core Protocol Engineer |
| **Testing Method** | (1) VERIFY `gas_limit = 50_000` → accepted. (2) VERIFY `gas_limit = 200_000` → rejected. (3) Non-VERIFY not subject to limit. (4) Threshold configurable. |
| **Definition of Done** | Tests pass; threshold configurable; reviewed. |

### Task 12.1.3 — Deployer Factory Allowlist

| Field | Detail |
|-------|--------|
| **Description** | When first frame is DEFAULT targeting a deployer, only allowlisted factory addresses accepted. Configurable allowlist. Reject unknown deployer as first DEFAULT frame target. |
| **Estimated Effort** | 3 story points |
| **Assignee/Role** | Core Protocol Engineer |
| **Testing Method** | (1) Allowlisted deployer → accepted. (2) Unknown deployer → rejected. (3) First frame is VERIFY → not subject to check. (4) Allowlist configurable. |
| **Definition of Done** | Tests pass; allowlist configurable; documented; reviewed. |

### Task 12.1.4 — Single Pending Transaction per Sender

| Field | Detail |
|-------|--------|
| **Description** | At most one frame tx pending per sender address (like EIP-7702). Second tx from same sender → rejected or replaced per gas bump rules. |
| **Estimated Effort** | 2 story points |
| **Assignee/Role** | Core Protocol Engineer |
| **Testing Method** | (1) Two frame txs from same sender → second rejected/replaced. (2) Mixed legacy + frame txs: legacy unaffected. (3) Different senders → both accepted. |
| **Definition of Done** | Tests pass; no mempool bloat; reviewed. |

### Task 12.1.5 — First VERIFY Frame Enforcement

| Field | Detail |
|-------|--------|
| **Description** | Soft policy (mempool only): first non-deployer frame must be VERIFY. Configurable. Block inclusion not affected. |
| **Estimated Effort** | 2 story points |
| **Assignee/Role** | Core Protocol Engineer |
| **Testing Method** | (1) VERIFY first → accepted. (2) SENDER first → rejected (strict mode). (3) DEFAULT deployer then VERIFY → accepted. (4) Policy disablable. |
| **Definition of Done** | Tests pass; configurable; block inclusion unaffected; reviewed. |

---

## EIP-8141 Reference Excerpts

### Security Considerations → Transaction Propagation

> Frame transactions introduce new denial-of-service vectors for transaction pools that node operators must mitigate. Because validation logic is arbitrary EVM code, attackers can craft transactions that appear valid during initial validation but become invalid later. Without any additional policies, an attacker could submit many transactions whose validity depends on some shared state, then submit one transaction that modifies that state, and cause all other transactions to become invalid simultaneously. This wastes the computational resources nodes spent validating and storing these transactions.

### Security Considerations → Example Attack

> A simple example is transactions that check `block.timestamp`:
>
> ```solidity
> function validateTransaction() external {
>     require(block.timestamp < SOME_DEADLINE, "expired");
>     // ... rest of validation
>     APPROVE(0x2);
> }
> ```
>
> Such transactions are valid when submitted but become invalid once the deadline passes, without any on-chain action required from the attacker.

### Security Considerations → Mitigations

> Node implementations should consider restricting which opcodes and storage slots validation frames can access, similar to ERC-7562. This isolates transactions from each other and limits mass invalidation vectors.
>
> It's recommended that to *validate* the transaction, a specific frame structure is enforced and the amount of gas that is expended executing the validation phase must be limited. Once the frame calls `APPROVE(0x2)`, it can be included in the mempool and propagated to peers safely.
>
> For deployment of the sender account in the first frame, the mempool must only allow specific and known deployer factory contracts to be used as `frame.target`, to ensure deployment is deterministic and independent of chain state.
>
> In general, it can be assumed that handling of frame transactions imposes similar restrictions as EIP-7702 on mempool relay, i.e. only a single transaction can be pending for an account that uses frame transactions.

# US-11.1 — ORIGIN Opcode Behavior Change

**Epic:** EP-11 ORIGIN Opcode Behavior Change
**Total Story Points:** 4
**Sprint:** 2

> **As a** protocol engineer,
> **I want** the `ORIGIN` opcode to return the frame's caller (ENTRY_POINT or tx.sender) for frame transactions,
> **so that** contracts that use ORIGIN for identification get a consistent, meaningful value.

**INVEST:** I ✓ | N ✓ | V ✓ | E ✓ | S ✓ | T ✓

---

## Tasks

### Task 11.1.1 — ORIGIN Opcode Override for Frame Transactions

| Field | Detail |
|-------|--------|
| **Description** | Modify `opOrigin`: when in frame transaction context, return the frame's caller address. DEFAULT/VERIFY → `ENTRY_POINT`. SENDER → `tx.sender`. Override applies throughout **all call depths** (sub-calls also see frame caller as ORIGIN). Check `FrameContext != nil` in EVM context. |
| **Estimated Effort** | 3 story points |
| **Assignee/Role** | EVM Engineer |
| **Testing Method** | (1) DEFAULT frame: `ORIGIN` → `ENTRY_POINT` at any call depth. (2) SENDER frame: `ORIGIN` → `tx.sender`. (3) VERIFY frame: `ORIGIN` → `ENTRY_POINT`. (4) Non-frame tx: unchanged. (5) Regression: existing ORIGIN tests still pass. |
| **Definition of Done** | Tests pass for all modes; non-frame txs unaffected; regression pass; reviewed. |

### Task 11.1.2 — Backward Compatibility Documentation

| Field | Detail |
|-------|--------|
| **Description** | Add comment in `opOrigin` and execution engine noting behavior change. Reference EIP-7702 precedent. Document `ORIGIN == CALLER` anti-pattern behavior under frame txs. |
| **Estimated Effort** | 1 story point |
| **Assignee/Role** | EVM Engineer |
| **Testing Method** | Code review; test asserting `ORIGIN != tx.origin` for frame transactions. |
| **Definition of Done** | Comment merged; test documents behavior change; reviewed. |

---

## Codebase Locations

| File | Relevance |
|------|-----------|
| `pkg/core/vm/instructions.go` (or equivalent) | `opOrigin` — currently returns traditional `tx.origin`, needs frame-aware override |
| `pkg/core/vm/eip8141_opcodes.go:38-56` | `FrameContext` — can be checked via `evm.FrameCtx != nil` |

## Implementation Status

**❌ Not Implemented**

- ❌ `opOrigin` not modified for frame transactions
- ❌ No `FrameContext` check in ORIGIN handler

---

## EIP-8141 Reference Excerpts

### Specification → Behavior (ORIGIN)

> - The `ORIGIN` opcode returns frame `caller` throughout all call depths.

### Backwards Compatibility

> The `ORIGIN` opcode behavior changes for frame transactions, returning the frame's caller rather than the traditional transaction origin. This is consistent with the precedent set by EIP-7702, which already modified `ORIGIN` semantics. Contracts that rely on `ORIGIN = CALLER` for security checks (a discouraged pattern) may behave differently under frame transactions.

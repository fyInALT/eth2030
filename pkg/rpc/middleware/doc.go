// Package middleware provides JSON-RPC server middleware components including
// per-client rate limiting with token bucket algorithm and admin lifecycle
// management for graceful server startup and shutdown.
package middleware
